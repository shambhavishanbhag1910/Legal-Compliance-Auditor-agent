services:
  auditor-ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
    env_file:
      - .env
    environment:
      STORAGE_BACKEND: local
      LOCAL_DATA_DIR: /app/data
    ports:
      - "8000:8000"
    volumes:
      - auditor_ui_data:/app/data

volumes:
  auditor_ui_data:
